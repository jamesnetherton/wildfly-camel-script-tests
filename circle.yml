machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t jamesnetherton/wildfly-camel-script-tests .
test:
  override:
    - docker run  jamesnetherton/wildfly-camel-script-tests
